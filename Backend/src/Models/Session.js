class Session {
    constructor(id_session, user, name, creation_date, last_update) {
        this.id_session = id_session
        this.user = user
        this.name = name
        this.creation_date = creation_date
        this.last_update = last_update
    }
}

export default Session