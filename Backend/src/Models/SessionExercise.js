class SessionExercise {
    constructor(app_exercise, user, exercise, session) {
        this.app_exercise = app_exercise
        this.user = user
        this.exercise = exercise
        this.session = session
    }
}

export default SessionExercise