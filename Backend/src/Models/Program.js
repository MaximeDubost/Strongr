class Program {
    constructor(id, name, program_goal_name, session_count, volume) {
        this.id = id
        this.name = name
        this.program_goal_name = program_goal_name
        this.session_count = session_count
        this.volume = volume
    }
}

export default Program