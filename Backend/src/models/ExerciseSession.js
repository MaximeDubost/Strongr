class ExerciseSession {
    constructor(id, place, app_exercise_name, set_count, tonnage) {
        this.id = id
        this.place = place
        this.app_exercise_name = app_exercise_name
        this.set_count = set_count
        this.tonnage = tonnage
    }
}

export default ExerciseSession