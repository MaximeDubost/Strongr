class ExerciseSession {
    constructor(id, place, name_exercise, app_exercise_name, set_count, volume) {
        this.id = id
        this.place = place
        this.name = name_exercise
        this.app_exercise_name = app_exercise_name
        this.set_count = set_count
        this.volume = volume
    }
}

export default ExerciseSession